import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'خبرم کن' }

export default function StockAlertsLayout({ children }: { children: React.ReactNode }) {
  return children
}
