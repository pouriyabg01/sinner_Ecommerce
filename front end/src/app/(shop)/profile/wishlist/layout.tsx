import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'علاقه‌مندی‌ها',
  description: 'کالاهایی که برای بعد ذخیره کرده‌اید.',
}

export default function WishlistLayout({ children }: { children: React.ReactNode }) {
  return children
}
