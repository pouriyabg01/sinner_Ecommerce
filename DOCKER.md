# اجرای سایت با داکر

کل سایت در شش کانتینر بالا می‌آید:

| کانتینر | کارش |
|---|---|
| `nginx` | تنها در ورودی؛ درخواست را بین لاراول و Next پخش می‌کند و فایل‌های آپلودشده را می‌دهد |
| `frontend` | موتور صفحه‌های Next (خروجی `standalone`) |
| `backend` | لاراول روی PHP-FPM |
| `worker` | همان تصویر `backend`، ولی کارهای صف را برمی‌دارد (ارسال اطلاع‌رسانی ایمیلی) |
| `postgres` | پایگاه داده |
| `redis` | کش، نشست و صف |

مسیر هر درخواست:

```
بازدیدکننده → nginx ─┬─ /api/… و /up   → backend (لاراول) → postgres / redis
                     ├─ /storage/…     → فایل‌های آپلودشده (ولوم مشترک)
                     └─ بقیه           → frontend (صفحه‌های سایت)
```

## اجرای محلی

```bash
cp .env.example .env      # اولین بار
docker compose up -d --build
```

سایت روی `http://localhost:8080` بالا می‌آید. برای پر کردن پایگاه داده‌ی خالی با داده‌ی نمونه، یک بار
`DB_SEED=true` را در `.env` بگذارید و `docker compose up -d` بزنید، بعد دوباره `false` کنید.

دستورهای پرکاربرد:

```bash
docker compose logs -f backend        # گزارش خطاهای لاراول
docker compose ps                     # وضعیت کانتینرها
docker compose exec backend php artisan tinker
docker compose down                   # توقف (داده‌ها می‌مانند)
docker compose down -v                # توقف + پاک کردن کامل داده‌ها
```

## متغیرهای مهم `.env`

| متغیر | توضیح |
|---|---|
| `PUBLIC_URL` | نشانی عمومی سایت، بدون اسلش آخر. **با تغییرش باید تصویر جلویی دوباره ساخته شود** |
| `HTTP_PORT` | درگاهی که انجین‌ایکس می‌گیرد؛ روی سرور معمولاً `80` |
| `APP_KEY` | کلید رمزنگاری لاراول؛ اگر عوض شود، داده‌های رمزشده‌ی قبلی باز نمی‌شوند |
| `DB_PASSWORD` | رمز پستگرس؛ روی سرور حتماً یک رمز تازه بگذارید |
| `DB_SEED` | فقط بار اول و فقط روی پایگاه داده‌ی خالی |

`PUBLIC_URL` هنگام **ساخت** داخل کد مرورگر جاسازی می‌شود (قانون `NEXT_PUBLIC_`)، پس با عوض کردنش
`docker compose build frontend` لازم است. رندر سمت سرور از این آدرس استفاده نمی‌کند؛ آن از
`INTERNAL_API_URL` یعنی `http://nginx/api/v1` می‌رود که داخل شبکه‌ی داکر است.

## استقرار روی سرور (اوبونتو)

### ۱. دامنه را به سرور وصل کنید

در پنل دامنه، دو رکورد `A` بسازید — یکی برای خود دامنه و یکی برای `www` — هر دو به نشانی سرور.
تا پخش شدنش معمولاً بین چند دقیقه تا چند ساعت طول می‌کشد.

### ۲. آماده‌سازی سرور

اسکریپت `scripts/server-setup.sh` را روی سرور و با کاربر ریشه اجرا کنید:

```bash
scp scripts/server-setup.sh root@<نشانی-سرور>:/root/
ssh root@<نشانی-سرور> 'bash /root/server-setup.sh sinner'
```

این اسکریپت سیستم را به‌روز می‌کند، کاربر `sinner` می‌سازد، دو گیگابایت فضای مبادله اضافه می‌کند
(برای مرحله‌ی ساخت لازم است)، دیوار آتش را روی ۲۲ و ۸۰ و ۴۴۳ می‌بندد و داکر را نصب می‌کند.

### ۳. فرستادن پروژه

```bash
rsync -avz --exclude node_modules --exclude .next --exclude vendor --exclude .env \
  ./ sinner@<نشانی-سرور>:~/SInner/
```

### ۴. تنظیمات روی سرور

```bash
ssh sinner@<نشانی-سرور>
cd ~/SInner
cp .env.example .env
nano .env
```

| مقدار | چه بگذارید |
|---|---|
| `PUBLIC_URL` | `https://<دامنه>` |
| `HTTP_PORT` | `80` |
| `HTTPS_PORT` | `443` |
| `APP_KEY` | **همان کلید دستگاه خودتان** اگر داده منتقل می‌کنید (در فایل `APP_KEY.txt` بسته‌ی داده هست) |
| `DB_PASSWORD` | یک رمز تازه و طولانی |
| `DB_SEED` | `false` |

### ۵. بالا آوردن

```bash
docker compose up -d --build
```

اولین ساخت روی سرور چند دقیقه طول می‌کشد. بعدش سایت روی `http://<دامنه>` بالاست.

### ۶. انتقال داده‌ی دستگاه خودتان

روی رایانه‌ی خودتان:

```bash
bash scripts/export-data.sh
rsync -avz deploy-data-XXXXXX/ sinner@<نشانی-سرور>:~/SInner/deploy-data-XXXXXX/
```

روی سرور:

```bash
cd ~/SInner && bash scripts/import-data.sh deploy-data-XXXXXX
```

### ۷. گواهی امنیتی

گواهی را از پنل میزبان بگیرید و دو فایلش را روی سرور در `docker/nginx/certs/` بگذارید با همین نام‌ها:

```
fullchain.pem   گواهی + زنجیره‌ی صادرکننده
privkey.pem     کلید خصوصی
```

بعد در `.env` بنویسید `NGINX_CONF=https.conf` و بزنید:

```bash
docker compose up -d nginx
```

از این به بعد درگاه ۸۰ خودش به نسخه‌ی امن هدایت می‌کند. اگر گواهی تاریخ انقضا دارد، بعد از هر
تمدید فقط فایل‌ها را جایگزین کنید و همین دستور را دوباره بزنید.

### ۸. تنظیم‌های داخل پنل

این‌ها اصلاً به سرور کار ندارند و از «پیکربندی» پنل انجام می‌شوند:

- **درگاه پرداخت** — شناسه‌ی پذیرنده‌ی درگاهی که قرارداد دارید
- **سرویس پیامک** — کلید سامانه‌ی پیامکی برای کد ورود و اطلاع‌رسانی سفارش
- **سرویس ایمیل** — نشانی و رمز سرویسی که نامه‌ها را می‌فرستد

تا وقتی سرویس ایمیل تنظیم نشده، صفحه‌ی «ایمیل» در حالت آزمایشی می‌ماند: هیچ نامه‌ای
بیرون نمی‌رود و همه‌شان فقط در گزارش همان صفحه ثبت می‌شوند.

ارسال مستقیم از خود سرور جواب نمی‌دهد: درگاه ارسال روی سرورهای ابری معمولاً بسته
است و نشانی تازه‌ای که سابقه ندارد به پوشه‌ی هرزنامه می‌رود. باید سرویس ارسال بگیرید
و اطلاعاتش را در همان صفحه وارد کنید.

### ۹. پشتیبان‌گیری خودکار

```bash
crontab -e
```

و این خط، که هر شب ساعت سه پشتیبان می‌گیرد و نسخه‌های قدیمی‌تر از ۱۴ روز را پاک می‌کند:

```
0 3 * * * cd ~/SInner && SOURCE=docker bash scripts/export-data.sh >/dev/null 2>&1 && find ~/SInner -maxdepth 1 -name 'deploy-data-*' -mtime +14 -exec rm -rf {} +
```

## به‌روزرسانی سایت

```bash
docker compose up -d --build          # کد تازه؛ مهاجرت‌ها خودکار اجرا می‌شوند
docker image prune -f                 # پاک کردن لایه‌های قدیمی تا دیسک پر نشود
```

کانتینر بک‌اند هنگام بالا آمدن، منتظر پایگاه داده می‌ماند، `migrate --force` می‌زند، پیوند
`public/storage` را می‌سازد و تنظیمات و مسیرها را کش می‌کند.
